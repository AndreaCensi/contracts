language: python
reset_minion: true

python:
  - 2.7
  - 3.2
  - 3.3
  - 3.4
  - pypy
  
install:
  - pip install nose pyparsing decorator

script:
  - nosetests contracts

notifications:
  email:
   - censi@mit.edu
